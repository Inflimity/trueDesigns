// features/pricing-jump.js - Pricing anchor jump bar functionality

import { $ } from '../utils/helper.js';

export const initPricingJump = () => {
    const bar = $("#pricingJump");
    if (!bar) return;

    // Pricing jump bar is sticky via CSS
    // No additional JavaScript logic needed for basic functionality
    // Could add scroll spy highlighting here if needed in the future
};